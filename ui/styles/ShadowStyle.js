import colors from "../../constants/colors"

const shadowStyle = {
  shadowColor: colors.BASIC.BLACK,
  shadowOffset: {
    height: 3,
    width: 3
  },
  shadowOpacity: 0.2,
  shadowRadius: 4,
}

export default shadowStyle